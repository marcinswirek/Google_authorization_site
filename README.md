# zadanie-17-6-html-templates

```
'npm install' to install necessary npm packages
'npm start' to start and app watching using Nodemon
-enter link: http://localhost:3000/

-Previous push of this repo failed. I have no idea why, but git pushed this repo into totally different git repo, so I had to copy script from that repo files, and made one more.
```
